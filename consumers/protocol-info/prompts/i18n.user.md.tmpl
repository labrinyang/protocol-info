Translate the following JSON payload to **{{LOCALE_NAME}}** (locale code: `{{LOCALE_CODE}}`).

Return a JSON object with the same keys and the same `members` array length
and order. If a field is absent from a member object, leave it absent; it
represents a null source value and must not be filled. Apply the translation
rules from the system prompt.
{{GLOSSARY_SECTION}}

Source:

```json
{{SOURCE_JSON}}
```
