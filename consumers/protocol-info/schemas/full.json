{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "earn-protocol-info.schema.json",
  "title": "EarnProtocolInfo (crawler output)",
  "description": "Aligned with IEarnProtocolInfo from server-service-dashboard (entity + Create/Update/Import DTOs). `establishment` is required here (crawler must find founding year) and `sources` is a non-DB audit trail stripped before DB import.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "provider",
    "displayName",
    "type",
    "description",
    "tags",
    "establishment",
    "members",
    "providerWebsite",
    "providerXLink",
    "providerDiscordLink",
    "status",
    "fundingRounds",
    "audits"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{1,63}$",
      "description": "Business unique key. Per decision: equal to provider code, one per provider."
    },
    "provider": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9_-]{0,63}$",
      "description": "Provider code. Should match EProvider enum in server-service-earn/src/common/index.ts (no longer enforced at script level)."
    },
    "displayName": { "type": "string", "minLength": 1, "maxLength": 80 },
    "type": {
      "type": "string",
      "enum": ["fixed_rate", "simple_earn", "staking"]
    },
    "description": { "type": ["string", "null"], "maxLength": 1000 },
    "tags": {
      "type": "array",
      "items": { "type": "string", "minLength": 1, "maxLength": 32 },
      "minItems": 1,
      "maxItems": 20
    },
    "establishment": {
      "type": "integer",
      "minimum": 1900,
      "maximum": 2030,
      "description": "Founding year. Required — crawler must find it (stricter than dashboard DTO which treats it as optional). Range matches dashboard's 1900~currentYear+1 with reasonable headroom; bump max yearly."
    },
    "members": {
      "type": "array",
      "minItems": 1,
      "maxItems": 30,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "memberName",
          "memberPosition",
          "oneLiner",
          "avatarUrl",
          "memberLink"
        ],
        "properties": {
          "memberName": { "type": "string", "minLength": 1, "maxLength": 80 },
          "memberPosition": { "type": "string", "maxLength": 80 },
          "oneLiner": {
            "type": ["string", "null"],
            "maxLength": 140,
            "description": "One sentence (≤ 140 chars) on notable prior experience. Null only when nothing verifiable can be found."
          },
          "avatarUrl": {
            "type": ["string", "null"],
            "format": "uri",
            "maxLength": 500
          },
          "memberLink": {
            "type": "object",
            "additionalProperties": false,
            "required": ["xLink", "linkedinLink"],
            "properties": {
              "xLink": {
                "type": ["string", "null"],
                "format": "uri",
                "maxLength": 500
              },
              "linkedinLink": {
                "type": ["string", "null"],
                "format": "uri",
                "maxLength": 500
              }
            }
          }
        }
      }
    },
    "providerWebsite": {
      "type": "string",
      "format": "uri",
      "maxLength": 500
    },
    "providerXLink": {
      "type": "string",
      "format": "uri",
      "maxLength": 500
    },
    "providerDiscordLink": {
      "type": ["string", "null"],
      "format": "uri",
      "maxLength": 500
    },
    "status": {
      "type": "string",
      "enum": ["draft", "active", "archived"],
      "description": "Lifecycle state (aligned with EProtocolInfoStatus). Crawler output should be 'draft'; human promotes to 'active'/'archived' via dashboard."
    },
    "fundingRounds": {
      "type": "array",
      "maxItems": 20,
      "description": "Full funding history, newest round first. If the protocol has raised Series B, Seed and Series A MUST also be present — never emit only the latest round.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["round", "date", "amount", "valuation", "investors"],
        "properties": {
          "round": {
            "type": "string",
            "minLength": 1,
            "maxLength": 80,
            "description": "Free-form label: 'Seed', 'Pre-Seed', 'Series A', 'Series B', 'Strategic', 'Private', 'Public', 'Grant', etc."
          },
          "date": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}(-\\d{2})?$",
            "description": "ISO date YYYY-MM-DD when the exact day is known, otherwise YYYY-MM."
          },
          "amount": {
            "type": ["string", "null"],
            "maxLength": 32,
            "description": "Figma display string with currency, e.g. '$165M', '$11M'. Null when undisclosed."
          },
          "valuation": {
            "type": ["string", "null"],
            "maxLength": 32,
            "description": "Figma display string, e.g. '$1.66B'. Null when undisclosed."
          },
          "investors": {
            "type": "array",
            "maxItems": 50,
            "items": { "type": "string", "minLength": 1, "maxLength": 120 },
            "description": "Participating firms/angels for this round. Empty array when undisclosed."
          }
        }
      }
    },
    "audits": {
      "type": "object",
      "additionalProperties": false,
      "required": ["items", "lastScannedAt"],
      "description": "Security audit history plus the crawler's own scan timestamp (document-level updatedAt is not granular enough).",
      "properties": {
        "items": {
          "type": "array",
          "maxItems": 30,
          "description": "One entry per audit report, newest first.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "auditor",
              "auditorLogoUrl",
              "date",
              "scope",
              "reportUrl"
            ],
            "properties": {
              "auditor": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120,
                "description": "Audit firm name, e.g. 'Certora', 'Trail of Bits', 'OpenZeppelin', 'Spearbit'."
              },
              "auditorLogoUrl": {
                "type": ["string", "null"],
                "format": "uri",
                "maxLength": 500
              },
              "date": {
                "type": "string",
                "pattern": "^\\d{4}-(0[1-9]|1[0-2])(-(0[1-9]|[12]\\d|3[01]))?$",
                "description": "Audit report date. Must be YYYY-MM or YYYY-MM-DD; bare years are invalid. If only a year appears in an audit folder name, open the report or announcement to recover the month.",
                "examples": ["2024-05", "2022-11-15", "2024-01"]
              },
              "scope": {
                "type": ["string", "null"],
                "maxLength": 200,
                "description": "What was audited — used to disambiguate when the same firm audited multiple times. Null if unknown."
              },
              "reportUrl": {
                "type": ["string", "null"],
                "format": "uri",
                "maxLength": 500,
                "description": "Link to the audit report PDF/blog post."
              }
            }
          }
        },
        "lastScannedAt": {
          "type": "string",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
          "description": "UTC date the audits block was scanned. Claude may leave any YYYY-MM-DD value; the protocol-info-final normalizer (consumers/protocol-info/normalizers/final.mjs) overwrites this with today's UTC date before validation."
        }
      }
    },
    "sources": {
      "type": "array",
      "description": "Non-DB audit trail: URLs Claude cited. Stripped before DB import.",
      "items": { "type": "string", "format": "uri" }
    }
  }
}
